import 'dart:io';
import 'package:path/path.dart' as p;

class ModelGenerator {
  final String name;
  ModelGenerator(this.name);
  void generate(Map<String, dynamic> data) {
    print('📦 Model generator stub for $name in CLI-MANAGER');
  }
}
